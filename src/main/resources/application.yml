server:
  port: 8081

spring:
  application:
    name: stock-service

  datasource:
    url: jdbc:postgresql://localhost:5434/stock_db
    username: tez_user
    password: tez_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health, metrics, prometheus
  metrics:
    tags:
      application: ${spring.application.name}